import React from "react";
import { Layout } from "antd";

const { Header } = Layout;

const AppHeader = () => (
  <Header className="header">
    <h1>Redux Demo</h1>
  </Header>
);

export default AppHeader;
