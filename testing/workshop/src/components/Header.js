import React from "react";
import { Layout } from "antd";

const { Header } = Layout;

const AppHeader = () => (
  <Header className="header">
    <h1>React Testing</h1>
  </Header>
);

export default AppHeader;
